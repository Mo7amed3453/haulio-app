package app.haulio.shared.crime

/**
 * Local stub for the KMM shared HighRiskAlertEvent model.
 * Replace with actual KMM artifact when the shared module is wired.
 */
data class HighRiskAlertEvent(
    val cellId: String,
    val topCrimeType: String?,
)
