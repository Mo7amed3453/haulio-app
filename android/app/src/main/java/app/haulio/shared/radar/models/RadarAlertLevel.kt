package app.haulio.shared.radar.models

/**
 * Local stub for the KMM shared RadarAlertLevel enum.
 * Replace with actual KMM artifact when the shared module is wired.
 */
enum class RadarAlertLevel {
    VISUAL_FAR,
    AUDIO_MID,
    URGENT_CLOSE,
}
