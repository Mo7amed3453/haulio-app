package app.haulio.shared.navigation.models

/**
 * A latitude/longitude pair in decimal degrees.
 * Local Android stub — mirrors the KMM shared class until the shared module is wired in.
 */
data class GeoPoint(
    val lat: Double,
    val lon: Double,
)
